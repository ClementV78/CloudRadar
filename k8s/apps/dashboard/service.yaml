apiVersion: v1
kind: Service
metadata:
  name: dashboard
  namespace: cloudradar
  labels:
    app.kubernetes.io/name: dashboard
    app.kubernetes.io/part-of: cloudradar
spec:
  selector:
    app.kubernetes.io/name: dashboard
  ports:
    - name: http
      port: 80
      targetPort: http
