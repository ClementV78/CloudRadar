<?xml version="1.0"?>
<ruleset name="CloudRadar"
    xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0
        https://pmd.sourceforge.io/ruleset_2_0_0.xsd">

  <description>
    CloudRadar shared PMD ruleset — catches god-class and design smells.
    Ref: https://github.com/ClementV78/CloudRadar/issues/557
  </description>

  <!-- Known violations excluded until refactoring.
       Each exclusion links to a tracking issue. -->
  <!-- #558: https://github.com/ClementV78/CloudRadar/issues/558 -->
  <exclude-pattern>.*/RedisAggregateProcessor\.java</exclude-pattern>
  <!-- #559: https://github.com/ClementV78/CloudRadar/issues/559 -->
  <exclude-pattern>.*/FlightIngestJob\.java</exclude-pattern>
  <exclude-pattern>.*/OpenSkyTokenService\.java</exclude-pattern>
  <exclude-pattern>.*/OpenSkyClient\.java</exclude-pattern>
  <!-- #560: https://github.com/ClementV78/CloudRadar/issues/560 -->
  <exclude-pattern>.*/FlightQueryService\.java</exclude-pattern>
  <!-- Properties classes are getter/setter POJOs, not design-smell targets -->
  <exclude-pattern>.*Properties\.java</exclude-pattern>
  <!-- TODO: QueryParser has high cyclomatic complexity in parsing methods -->
  <exclude-pattern>.*/QueryParser\.java</exclude-pattern>
  <!-- TODO: review coupling in photo + metrics services -->
  <exclude-pattern>.*/PlanespottersPhotoService\.java</exclude-pattern>
  <exclude-pattern>.*/PrometheusMetricsService\.java</exclude-pattern>

  <!-- God Class: high WMC + ATFD + TCC thresholds -->
  <rule ref="category/java/design.xml/GodClass"/>

  <!-- Too many methods in a single class -->
  <rule ref="category/java/design.xml/TooManyMethods">
    <properties>
      <property name="maxmethods" value="15"/>
    </properties>
  </rule>

  <!-- Cyclomatic complexity per method -->
  <rule ref="category/java/design.xml/CyclomaticComplexity">
    <properties>
      <property name="methodReportLevel" value="12"/>
      <property name="classReportLevel" value="80"/>
    </properties>
  </rule>

  <!-- Coupling between objects -->
  <rule ref="category/java/design.xml/CouplingBetweenObjects">
    <properties>
      <property name="threshold" value="15"/>
    </properties>
  </rule>

  <!-- Excessive public count (proxy for god-class surface area) -->
  <rule ref="category/java/design.xml/ExcessivePublicCount">
    <properties>
      <property name="minimum" value="20"/>
    </properties>
  </rule>
</ruleset>
