apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthz
  namespace: cloudradar
  labels:
    app.kubernetes.io/name: healthz
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: healthz
  template:
    metadata:
      labels:
        app.kubernetes.io/name: healthz
    spec:
      serviceAccountName: healthz
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
      containers:
        - name: healthz
          image: ghcr.io/clementv78/cloudradar-health:0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8080
              name: http
          readinessProbe:
            httpGet:
              path: /healthz
              port: http
          livenessProbe:
            httpGet:
              path: /healthz
              port: http
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
